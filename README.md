# NetPackets NFT Minter

Автоматический скрипт для минта NFT из контракта NetPackets на Base сети.

## 📋 Требования

- Python 3.8+
- Приватный ключ кошелька с ETH (для gas) и USDC на Base сети
- Каждый минт требует 1 USDC

## 🚀 Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

3. Заполните `.env` файл:
```env
BASE_RPC_URL=https://mainnet.base.org
PRIVATE_KEY=ваш_приватный_ключ_без_0x
MINT_COUNT=5

# Опционально: автоматическая отправка NFT друзьям
TRANSFER_ENABLED=false
TRANSFER_ADDRESSES=0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb1,0x123...abc
```

## 💡 Использование

Запустите скрипт:
```bash
python mint_netpackets.py
```

Скрипт автоматически:
1. Проверит баланс USDC
2. Одобрит расходование USDC контрактом
3. Выполнит указанное количество минтов
4. (Опционально) Отправит NFT на адреса друзей
5. Покажет ссылки на транзакции в BaseScan

## ⚙️ Параметры

- `BASE_RPC_URL` - RPC endpoint для Base сети
- `PRIVATE_KEY` - приватный ключ вашего кошелька (БЕЗ префикса 0x)
- `MINT_COUNT` - количество NFT для минта
- `TRANSFER_ENABLED` - включить автоматическую отправку NFT (true/false)
- `TRANSFER_ADDRESSES` - список адресов получателей через запятую

## 🎁 Автоматическая отправка друзьям

Чтобы автоматически отправлять отминченные NFT друзьям, добавьте в `.env`:

```env
TRANSFER_ENABLED=true
TRANSFER_ADDRESSES=0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb1,0xAnotherAddress,0xYetAnotherAddress
```

Скрипт отправит по одному NFT на каждый адрес в порядке минта. Если адресов меньше чем NFT, оставшиеся останутся у вас.

## 📝 Контракты

- **NetPackets**: `0x4daBb4f0BCEc4Ece9fE4a8F5d709DA9CDc78bAE1`
- **USDC**: `0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913`

## ⚠️ Предупреждения

- Храните приватный ключ в безопасности
- Убедитесь, что у вас достаточно ETH для gas
- Убедитесь, что у вас достаточно USDC (1 USDC за каждый минт)
- Никогда не публикуйте файл `.env`

## 🔗 Полезные ссылки

- [BaseScan](https://basescan.org/)
- [Контракт NetPackets](https://basescan.org/address/0x4dabb4f0bcec4ece9fe4a8f5d709da9cdc78bae1)
